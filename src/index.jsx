import ReactDOM from 'react-dom';
import React from 'react';

const domContainer = document.querySelector('#app');

const App = () => <p>This is a placeholder</p>;

ReactDOM.render(<App />, domContainer);
